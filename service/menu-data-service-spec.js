describe('menuDataService', function() {

  beforeEach(module('stripeMenu'));

  it('should ...', inject(function(menuDataService) {

    //expect(menuDataService.doSomething()).toEqual('something');

  }));

});
